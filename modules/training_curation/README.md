This is a custom module for curation tasks in the SSHOC training inventory/toolkit.

Currently it covers:
* merging of terms (search for a term in a taxonomy and based on this apply a term for all found content)